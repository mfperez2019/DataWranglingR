library(readr)
library(readr)
library(dplyr)
library(lubridate)
titanic <- read_csv(file = "titanic_MD.csv")
titanic %>% View()
summary(titanic)
str_detect(
string = titanic$Sex,
pattern = "Miss|Mrs"
)
library(stringr)
str_detect(
string = titanic$Sex,
pattern = "Miss|Mrs"
)
str_detect(
string = titanic$Sex,
pattern = "(Miss)|(Mrs)"
)
str_detect(
string = titanic$Sex,
pattern = "(Miss)|(Mrs)?"
)
str_detect(
string = titanic$Name,
pattern = "(Miss)|(Mrs)"
)
str_detect(
string = titanic$Name,
pattern = "(Miss)|(Mrs)"
) %>% filter(TRUE)
